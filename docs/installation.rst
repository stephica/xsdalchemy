.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install xsdalchemy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv xsdalchemy
    $ pip install xsdalchemy
